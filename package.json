{
  "name": "transposerx",
  "private": true,
  "packageManager": "npm@10.9.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "migrate": "npm run migrate --workspace=apps/server"
  },
  "devDependencies": {
    "shadcn": "^4.7.0",
    "turbo": "^2.0.0"
  }
}
