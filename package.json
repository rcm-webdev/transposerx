{
  "name": "transposerx",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "migrate": "npm run migrate --workspace=apps/server"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
